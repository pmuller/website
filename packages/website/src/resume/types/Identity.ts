export type Identity = {
  firstName: string;
  lastName: string;
  // Yep, I know. It's weird to include my job title in my "identity"...
  // But it makes sense from an architectural point of view. \o/
  jobTitle: string;
};
